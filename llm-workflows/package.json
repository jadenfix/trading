{
  "name": "llm-workflows",
  "version": "1.0.0",
  "private": true,
  "description": "LLM trading workflows workspace metadata",
  "scripts": {
    "test": "cd llm-rules-bot && cargo test --workspace --all-targets"
  }
}
